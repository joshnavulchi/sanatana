name: Deploy to GitHub Pages (DISABLED)

# This workflow has been disabled by renaming the file to `.disabled`.
# To re-enable, remove the `.disabled` suffix or restore from git.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
